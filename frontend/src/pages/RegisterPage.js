import styled from 'styled-components';

const RegisterPageContainer = styled.div`
  height: 100vh;
  background: black;
`;

const RegisterPage = () => (
  <RegisterPageContainer>
    WELL DONE YOU ARE GRANTED ACCESS
  </RegisterPageContainer>
);

export default RegisterPage;
